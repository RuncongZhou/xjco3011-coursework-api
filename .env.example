# Optional: override default SQLite path (default: ./data/books.db under project root)
# DATABASE_URL=sqlite:///./data/books.db

# Optional: when set, POST / PATCH / DELETE require header: X-API-Key: <same value>
# API_KEY=your-secret-here
